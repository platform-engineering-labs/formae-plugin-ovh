/*
 * © 2025 Platform Engineering Labs Inc.
 *
 * SPDX-License-Identifier: FSL-1.1-ALv2
 */

amends "@formae/forma.pkl"

import "@ovh/storage/s3bucket.pkl"

import "./vars.pkl" as v

forma {
  v.stack
  v.target

  // Update the S3 bucket - change mutable property (versioning)
  new s3bucket.S3Bucket {
    label = "plugin-sdk-test-s3bucket"
    region = v.region // CreateOnly, no change
    name = "formae-test-bucket-\(v.testRunID)" // CreateOnly, no change
    encryption = new s3bucket.Encryption {
      sseAlgorithm = "AES256" // CreateOnly, no change
    }
    // Mutable: enable versioning
    versioning = new s3bucket.Versioning {
      status = "enabled"
    }
  }
}
